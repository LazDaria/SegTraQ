[project]
name = "segtraq"
version = "0.0.5"
description = "SegTraQ - A Python toolkit for quantitative and visual quality control of segmentation and transcript assignment in spatial omics data."
readme = "README.md"
authors = [
  {name = "Daria Lazic", email = "daria.lazic@embl.de"},
  {name = "Matthias Meyer-Bender", email = "matthias.meyerbender@embl.de"},
  {name = "Martin Emons", email = "martin.emons@uzh.ch"}
]
maintainers = [
  {name = "Daria Lazic", email = "daria.lazic@embl.de"},
  {name = "Matthias Meyer-Bender", email = "matthias.meyerbender@embl.de"},
  {name = "Martin Emons", email = "martin.emons@uzh.ch"}
]
classifiers = [

]
license = "MIT"
dependencies = [
  "typer",
  "scanpy>=1.12",  # not strictly required
  "spatialdata>=0.8.0",  # not strictly required
  "joblib",
  "geopandas",
  "rtree",
  "rasterio",
  "squidpy>=1.6.2",
  "tqdm",
  "anndata>=0.12", # to support zarr v3
  "ovrlpy>=1.2.0",
  "ome_zarr>=0.13.0",
  "setuptools<82.0.0"  # setuptools==82.0.0 dropped pkg_resources, which is needed by xarray and thus spatialdata
]
requires-python = ">= 3.12"

[project.optional-dependencies]
test = [
    "coverage",
    "pytest",
    "ruff",
    "ty",
    "ipdb",
    "igraph",
    "leidenalg",
    "black[jupyter]",
    "pytest-cov",
]

docs = [
    "sphinx",
    "sphinx-book-theme",  # or another theme you prefer
    "myst-parser",        # if you want to support markdown in Sphinx
    "nbsphinx",          # for rendering Jupyter notebooks in Sphinx
    "jupytext",
    "sphinx-autodoc-typehints",  # for better type hint rendering in
    "sphinx-copybutton",  # for copy buttons in code blocks
    "pandoc",
    "pygments",
    "ipython",
    "nbconvert",
    "ipykernel",
]

[project.urls]

bugs = "https://github.com/LazDaria/segtraq/issues"
changelog = "https://github.com/LazDaria/segtraq/blob/master/changelog.md"
homepage = "https://github.com/LazDaria/segtraq"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
exclude = ["tests", "tests.*", "docs", "examples", "data", "data.*"]

[project.scripts]
segtraq = "segtraq.cli:app"

[tool.ty]
# All rules are enabled as "error" by default; no need to specify unless overriding.
# Example override: relax a rule for the entire project (uncomment if needed).
# rules.TY015 = "warn"  # For invalid-argument-type, warn instead of error.

# Optional: Per-directory overrides for flexibility in boilerplates with tests or examples.
[[tool.ty.overrides]]
match = ["tests/**.py"]
rules.TY016 = "ignore"  # Ignore invalid-assignment in tests, e.g., for dynamic fixtures.
rules.TY029 = "ignore"  # Ignore invalid-return-type in tests.

[tool.ruff]
line-length = 120

[tool.black]
line-length = 120

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # Pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "UP",  # pyupgrade
]

# B018 ignores standalone expressions (e.g. used for printing at the end of a code block in a notebook)
# E402 ignores module level import not at top of file (e.g. used for imports in a notebook)
[tool.ruff.lint.per-file-ignores]
"docs/notebooks/*.py" = ["B018", "E402"]

[tool.uv]
package = true

[dependency-groups]
dev = [
    "ghp-import>=2.1.0",
]
